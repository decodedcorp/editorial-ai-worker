---
phase: 14-magazine-layout-variants
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - admin/src/components/blocks/pull-quote-block.tsx
  - admin/src/components/blocks/headline-block.tsx
  - admin/src/components/blocks/product-showcase-block.tsx
  - admin/src/components/blocks/celeb-feature-block.tsx
autonomous: true

must_haves:
  truths:
    - "Pull quote renders 5 distinct layouts including full-width background and oversized serif"
    - "Headline renders 4 distinct layouts including full-width banner and overlapping"
    - "Product showcase renders 6 distinct layouts including lookbook and featured+grid"
    - "Celeb feature renders 5 distinct layouts including spotlight and hero collage"
    - "All blocks fall back to current rendering when layout_variant is undefined/null"
  artifacts:
    - path: "admin/src/components/blocks/pull-quote-block.tsx"
      provides: "5 pull quote variants"
      contains: "layout_variant"
    - path: "admin/src/components/blocks/headline-block.tsx"
      provides: "4 headline variants"
      contains: "layout_variant"
    - path: "admin/src/components/blocks/product-showcase-block.tsx"
      provides: "6 product showcase variants"
      contains: "layout_variant"
    - path: "admin/src/components/blocks/celeb-feature-block.tsx"
      provides: "5 celeb feature variants"
      contains: "layout_variant"
  key_links:
    - from: "admin/src/components/blocks/pull-quote-block.tsx"
      to: "admin/src/lib/types.ts"
      via: "PullQuoteBlock.layout_variant type"
      pattern: "block\\.layout_variant"
    - from: "admin/src/components/blocks/product-showcase-block.tsx"
      to: "admin/src/lib/types.ts"
      via: "ProductShowcaseBlock.layout_variant type"
      pattern: "block\\.layout_variant"
---

<objective>
Implement layout variant rendering for supporting content blocks: pull_quote, headline, product_showcase, and celeb_feature.

Purpose: These blocks add editorial richness -- dramatic quotes, bold headlines, product merchandising, and celebrity features. Variants transform them from basic grids to magazine-quality layouts.
Output: Four fully variant-aware block components.
</objective>

<execution_context>
@/Users/kiyeol/.claude-pers/get-shit-done/workflows/execute-plan.md
@/Users/kiyeol/.claude-pers/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-magazine-layout-variants/14-01-SUMMARY.md
@admin/src/lib/types.ts
@admin/src/components/blocks/pull-quote-block.tsx
@admin/src/components/blocks/headline-block.tsx
@admin/src/components/blocks/product-showcase-block.tsx
@admin/src/components/blocks/celeb-feature-block.tsx
@admin/src/components/magazine-image.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull quote (5 variants) and Headline (4 variants)</name>
  <files>admin/src/components/blocks/pull-quote-block.tsx, admin/src/components/blocks/headline-block.tsx</files>
  <action>
**Pull quote variants** -- branch on `block.layout_variant ?? "default"`:

1. **`default`** (current): Keep exact current code -- border-left blockquote with 2xl-3xl italic text.

2. **`centered_large`**:
   - Remove border-left, center everything
   - `text-center py-12`
   - Quote text: `text-3xl md:text-4xl italic` with serif font
   - Decorative large curly quotes above/below (using pseudo or spans)
   - Attribution centered below

3. **`full_width_background`**:
   - Full-width colored background strip: `py-12 px-8`
   - Background: use designSpec accent color with low opacity, or `bg-gray-100`
   - `style={{ backgroundColor: accentColor + '15' }}` (15 = ~8% opacity hex)
   - Text centered, `text-2xl md:text-3xl italic text-center`

4. **`sidebar`**:
   - `float-right w-64 ml-6 mb-4 pl-4 border-l-2` with accent color
   - Smaller text: `text-lg italic`
   - Meant to sit alongside body_text blocks

5. **`oversized_serif`**:
   - Minimal decoration, no border, no quotes marks
   - Text: `text-5xl md:text-6xl font-serif leading-tight` with serif font
   - Very thin weight look: `text-gray-700`
   - Attribution in small caps below

**Headline variants** -- branch on `block.layout_variant ?? "default"`:

1. **`default`** (current): Keep exact current code with HEADING_STYLES and accent underline.

2. **`full_width_banner`**:
   - Full-width colored background: `py-10 px-8`
   - Background: designSpec primary color or `bg-gray-900`
   - Text: `text-4xl md:text-6xl font-bold text-white text-center`
   - No accent underline bar

3. **`left_aligned_large`**:
   - Oversized text: `text-6xl md:text-8xl font-bold tracking-tighter`
   - Left-aligned (no centering)
   - Serif font, `leading-[0.9]` for tight line height
   - Subtle accent color on first word or none

4. **`overlapping`**:
   - Semi-transparent, large overlapping text effect
   - `text-7xl md:text-9xl font-bold opacity-10` as background layer
   - Normal-sized text `text-3xl md:text-4xl font-bold` positioned on top
   - Use `relative` container with the large text `absolute inset-0` and overflow hidden
  </action>
  <verify>Run `cd /Users/kiyeol/development/decoded/editorial-ai-worker/admin && npx tsc --noEmit` -- zero errors.</verify>
  <done>Pull quote has 5 working variants. Headline has 4 working variants. Null/undefined = current rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Product showcase (6 variants) and Celeb feature (5 variants)</name>
  <files>admin/src/components/blocks/product-showcase-block.tsx, admin/src/components/blocks/celeb-feature-block.tsx</files>
  <action>
**Product showcase variants** -- branch on `block.layout_variant ?? "grid"`:

1. **`grid`** (current/default): Keep exact current code -- `grid grid-cols-2 sm:grid-cols-3` with Cards.

2. **`full_width_row`**:
   - Horizontal full-width layout: `flex gap-6 overflow-x-auto pb-3`
   - Each product: `flex-none w-48` with image on top, text below
   - No Card wrapper, cleaner look: just image + name + brand

3. **`featured_plus_grid`**:
   - First product hero-sized, rest in grid below
   - First: `col-span-full` with `aspect-[16/10]` image, large name text
   - Remaining: `grid grid-cols-2 sm:grid-cols-3 gap-4` (current Card style)

4. **`minimal_list`**:
   - Vertical list, no images
   - Each product: `flex justify-between items-center py-4 border-b`
   - Left: name (font-semibold) + brand (text-sm text-muted)
   - Right: description if present (text-sm)
   - Clean, text-only editorial feel

5. **`lookbook`**:
   - Alternating full-width image/text rows
   - Odd items: image left (60%), text right (40%)
   - Even items: text left (40%), image right (60%)
   - `grid grid-cols-1 md:grid-cols-5 gap-0` per item
   - Image: `md:col-span-3`, text: `md:col-span-2 flex items-center p-8`

6. **`carousel_cards`**:
   - Horizontal scroll cards with snap
   - `flex gap-4 overflow-x-auto snap-x snap-mandatory pb-3`
   - Each: `flex-none w-72 snap-start` with Card, taller image `aspect-[3/4]`
   - Subtle shadow and hover scale

**Celeb feature variants** -- branch on `block.layout_variant ?? "grid"`:

1. **`grid`** (current/default): Keep exact current code -- circular avatars in grid.

2. **`spotlight`**:
   - If 1+ celebs: first celeb gets spotlight treatment
   - `grid grid-cols-1 md:grid-cols-2 gap-8`
   - Left: large portrait (not circular, rounded-lg), `aspect-[3/4]`
   - Right: name `text-3xl font-bold`, description `text-lg leading-relaxed`
   - Additional celebs: small list below

3. **`card_row`**:
   - Horizontal tall portrait cards
   - `flex gap-4 overflow-x-auto pb-3`
   - Each: `flex-none w-56` with tall image `aspect-[2/3]`, rounded-lg
   - Name overlaid at bottom with gradient: `absolute bottom-0 inset-x-0 bg-gradient-to-t from-black/70 p-4`
   - `text-white font-semibold`

4. **`minimal_list`**:
   - Clean text list, no images
   - Each celeb: `flex items-center gap-4 py-3 border-b`
   - Small avatar circle `size-10` on left
   - Name + description text on right
   - Editorial clean style

5. **`hero_collage`**:
   - Overlapping photos full width
   - `relative h-[400px] md:h-[500px]`
   - Position images absolutely with varying sizes and positions
   - First: `w-[50%] top-0 left-0 z-10`
   - Second: `w-[45%] top-[15%] right-0 z-20`
   - Third: `w-[40%] bottom-0 left-[20%] z-30`
   - Each with `shadow-xl rounded-sm`
   - Names overlaid or listed below collage
  </action>
  <verify>Run `cd /Users/kiyeol/development/decoded/editorial-ai-worker/admin && npx tsc --noEmit` -- zero errors.</verify>
  <done>Product showcase has 6 working variants. Celeb feature has 5 working variants. Null/undefined = current rendering.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Each component has branch for every documented variant
3. Null/undefined layout_variant produces identical output to current code
</verification>

<success_criteria>
- Pull quote: 5 variants (centered_large = big centered quote, full_width_background = colored strip, sidebar = float-right, oversized_serif = massive serif)
- Headline: 4 variants (full_width_banner = dark bg, left_aligned_large = 8xl text, overlapping = semi-transparent layer)
- Product showcase: 6 variants (full_width_row, featured_plus_grid, minimal_list, lookbook, carousel_cards)
- Celeb feature: 5 variants (spotlight = portrait+text, card_row = tall cards, minimal_list, hero_collage = overlapping)
- All backward compatible
</success_criteria>

<output>
After completion, create `.planning/phases/14-magazine-layout-variants/14-03-SUMMARY.md`
</output>
